from datetime import *

class Alert:
    
    # Initializer
    def __init__(self, name, times, finalDate, dosage):
        self.name = name
        self.dates = dates
        self.times = times
        self.finalDate = finalDate
        self.dosage = dosage
        self.currentTime = now()
        self.currentDate = today()

    def checkTime(self):
        if currentTime.hour in times[currentDate] and currentDate < finalDate:
            doAlert()

    def doAlert(self):
        alertBool = True
        #pass this through
        alertBool = False
            
            


    
